version: '3.8'

services:
  search-engine:
    build: .
    container_name: nlp_search_engine
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
